# 阶段路线图

> 本文只维护阶段目标。任务状态、优先级、证据和验收标准统一维护在
> [任务台账](BACKLOG.md)，避免路线图、验收报告和设计文档各写一套状态。

## Phase 0：架构基线（已完成）

- Electron/Web 双宿主工程。
- 产品、信息架构、技术架构、数据库设计。
- 主题系统与平台端口。
- 书架、创建小说、章节规划、批量生成配置原型。

## Phase 1：本地写作闭环（已完成基础）

- [已完成基础] SQLite/libSQL Schema、数据库门面与启动迁移。
- [已完成章节部分] 章节正文编辑、自动保存和不可变版本快照。
- [已完成基础] 卷章场景完整 CRUD、章节增删与顺序调整。
- [已完成基础] 故事圣经核心文档，以及角色、地点、势力、物品、术语管理。
- [已完成基础] 时间线、伏笔、角色阶段状态及章节关联的动态正史台账。
- [已完成基础] Context Pack 构建器：按优先级组装故事圣经、实体、卷章场景、最近正文与动态正史，支持预算裁剪和不可变快照。
- [已完成基础] Token 用量账本：区分本地估算与供应商实测，预留缓存 Token、费用和模型维度。
- [已完成基础] 模型 Profile、Electron safeStorage 加密密钥、Web 会话密钥与 OpenAI-compatible 连接测试适配器。
- [已完成基础] 单章流式正文生成、候选稿持久化、真实 Token 回写以及接受/拒绝状态机。
- [已完成基础] 可恢复批量生成队列：逐章检查点、串行执行、暂停/恢复、失败重试、退出续跑与章节间 Token 预算门控。
- [已完成基础] Electron 当前请求即时取消、候选稿集中审阅以及批量接受/拒绝。
- [已完成基础] Electron 主进程后台调度器；页面切换不影响生成，进程退出后可按检查点恢复。
- [已完成基础] 候选稿确定性质量检查：字数、场景视角、地点与计划回收伏笔；只生成 Finding，不自动改正史。
- [已完成] Finding 持久化并接入 AI 结构化事实提取，作者确认后再更新正史。
- [已完成基础] Finding 集中审阅 UI，支持逐项解决与忽略。
- [已完成基础] AI 结构化事实提取：严格 JSON 边界、建议持久化、独立 Token 记录，不自动入正史。
- [已完成] 事实建议审阅 UI：逐项接受/拒绝，接受后写入时间线、角色状态或伏笔台账；角色按名称和别名匹配，不自动创建错误正史。
- [已完成] 项目备份与导出恢复：TXT、Markdown、v1 完整项目数据包导出；数据包校验后可作为新作品恢复，重建跨表引用且不覆盖原项目。
- [已完成] Windows 交付基线：Amy Novel 品牌图标、首次启动引导、NSIS 安装配置与发布说明。
- [已完成] 0.1.0 发布验收：打包版启动冒烟、完整测试、诊断报告导出、Apache-2.0 LICENSE 与 NOTICE。
- [已完成] NSIS 独立临时目录安装、隔离 Profile 启动、卸载与清理闭环。

## Phase 2：AI 批量生成闭环（当前）

- 复用 Amy-PPT 模型配置思想，实现 OpenAI-compatible 首个适配器。
- Context Pack、章节生成状态机、暂停/恢复、失败重试。
- 候选稿 Diff、接受/拒绝、连续性检查、事实提取。
- Token/费用统计与硬预算。

### Workflow Runner 前置可靠性（2026-08-30）

- [x] 候选稿未接受时禁止事实建议进入正史。
- [x] Web/Electron 共用 OpenAI-compatible URL、请求体和供应商私有参数规则。
- [x] 429/5xx `Retry-After` / 指数退避、单批次单执行器和剩余 Token 硬限制。
- [x] 串行批次候选链：后章承接前章候选，但候选链不进入正史。
- [x] 按 `ROLLING_STORY_HARNESS.md` 完成规划日志、十章周期、范围门禁、增补提案和人物动态记忆基础。
- [x] 使用真实 API 完成 `1–10 → 11–20` 两个规划周期（GLM-5.2 无头驱动闭环，`E2E_ROLLING_CYCLE.md`）：状态机、门禁、正史与记忆链路通过；发现事实提案 payload 形状、实体重复、下一周期规划上下文缺口和裸 JSON 失败率四类问题，其中两项解析边界已修复。
- [x] 事实提案 payload 容错、封存记忆注入、记忆审核硬门禁、一次低温 JSON 修复、实体短引用/合并与跨周期去重均已完成，见 `BACKLOG.md` AN-001～AN-005。

## 2026-08-30 交接事项审计（2026-08-31 更新）

原交接事项仍未全部完成：文风模板、事实提案容错、封存记忆注入和记忆审核门禁已完成；
一次低温 JSON 修复、实体短引用/去重也已完成；Workflow Runner 未完成。逐项状态和
验收标准见 `BACKLOG.md` AN-001～AN-005、AN-020、AN-022。

## Phase 2.5：AI 全自动小说工作流（Autopilot Agent）

设计详见 `AUTOPILOT_DESIGN.md`。目标：作者只出 idea 并终审，其余全部 Agent 接力。

- [已完成] idea → AI 生成故事圣经 + 角色/实体卡（规划页按钮，zod 校验落库）。
- [x] 旧版一次生成全部章节标题与章纲已改为全书路线骨架 + 当前可配置批次策划包。
- [x] P3.1 十步审核式规划向导：缺失信息逐项追问、当前步骤聚焦、人工确认解锁、一致性门禁后进入正文生成。
- [x] P3.1 人物分层体系：主角/核心配角/酱油人物/跑龙套四级卡片强度，酱油仅章纲提及注入，龙套不进正史。
- [x] P3.1 题材名称库：姓/名分池 + 与既有实体和别名查重，所有已采用人物名进入已用名集合。
- [x] P3.1 场景卡与复用基础：location 实体扩展功能/氛围/视觉锚点/常驻人物/危险等级，同一场景按章纲相关性注入。
- [ ] P3.2 Workflow Runner：一键全自动流水线（圣经→人物→场景→卷章→逐章），三档检查点模式，workflow_runs 断点恢复。
- [ ] P3.3 审查驱动自动重写（error 带反馈重生成 ≤N 次）与达标自动采纳（默认关，标记可回滚）。
- [x] P3.4 上下文相关性基础：人物/场景按章纲提及过滤注入，酱油不占常驻 token 预算。
- [x] P3.5 滚动十章 Harness 基础：周期状态机、当前范围门禁、预期/实际结束状态对账。
- [进行中] P3.6 版本化记忆：人物按章状态与技能已落地；章节摘要、通用实体状态、实体关系及改写失效传播待补。
- [x] P3.7 文风模板库：样章与分析仅本地保存；正文生成只注入抽象风格规范，不注入来源情节和原句；Electron/Web、单章/批量入口及测试已接通。

## Phase 3：增强与生态

- DOCX、EPUB、向量检索、关系图、时间线。
- DeepSeek/Anthropic/Gemini/Ollama 原生适配。
- 封面、人物图、TTS、发布前质量检查。
- Skill/Prompt 包与插件接口。

## 发布门槛

- 任何批量生成任务可在异常退出后恢复。
- 任何 AI 写入可追溯、可拒绝、可通过版本恢复。
- API Key 不出现在数据库、日志和项目备份中。
- [已通过] Windows 0.1.0 安装包通过隔离临时环境安装/启动/卸载冒烟测试；当前 Web 与 Electron 生产构建通过。
- [待补] PlatformPort 双端契约测试与 Renderer 关键流程测试，见 `BACKLOG.md` AN-010、AN-011。
